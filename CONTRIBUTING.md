# Contributing

Use Node 22.23.2 and pnpm 10.30.3. Run `pnpm verify` before opening a signed pull request. All third-party GitHub Actions must be pinned to immutable commit SHAs and workflows must declare minimal permissions.
