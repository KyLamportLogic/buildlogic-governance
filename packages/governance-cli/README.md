# @kypython/buildlogic-governance-cli

Run deterministic governance helpers from the command line.

```sh
buildlogic-governance validate-contract '{"intent":"example action","logic_constraints":["no network"],"assumptions":[],"fallback_strategy":"abort"}'
```
