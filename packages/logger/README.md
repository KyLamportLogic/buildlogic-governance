# @kypython/buildlogic-logger

Structured application logging with optional Sentry and New Relic integrations. New Relic is loaded only when `NEW_RELIC_APP_NAME`, `NEW_RELIC_LICENSE_KEY`, or `NEW_RELIC_CONFIG_FILE` is configured.
